/**
 * Test case for npmBin.
 * Runs with mocha.
 */
'use strict'

const npmBin = require('../lib/npm_bin.js'),
    assert = require('assert');

describe('npm-bin', () => {

    before((done) => {
        done();
    });

    after((done) => {
        done();
    });


    it('Npm bin', (done) => {
        done();
    });
});

