/**
 * Test case for spawnProcess.
 * Runs with mocha.
 */
'use strict'

const spawnProcess = require('../lib/processing/spawn_process.js')
const assert = require('assert')

describe('spawn-process', () => {
  before((done) => {
    done()
  })

  after((done) => {
    done()
  })

  it('Spawn process', (done) => {
    done()
  })
})

/* global describe, before, after, it */
