/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const execcli = require('./execcli')
const npmBin = require('./npm_bin')

let lib = execcli.bind(this)

Object.assign(lib, {
  execcli,
  npmBin
})

module.exports = lib
