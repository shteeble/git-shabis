/**
 * Test case for argx.
 * Runs with mocha.
 */
'use strict'

const argx = require('../lib/argx.js')
const assert = require('assert')

describe('argx', () => {
  before((done) => {
    done()
  })

  after((done) => {
    done()
  })

  it('Argx', (done) => {
    done()
  })
})

