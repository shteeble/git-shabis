'use strict'

const {
{{#each usages}}
  {{name}}{{#if @last}}{{else}},
{{/if}}
{{/each}}

} = require('stringcase')

{{#each usages}}
// ------------------------
// Convert to {{name}}
// ------------------------
{{#each samples}}{{../name}}('{{src}}') // => "{{converted}}"
{{/each}}

{{/each}}