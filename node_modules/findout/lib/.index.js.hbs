/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */
'use strict'

const findout = require('./findout')
const resolve = require('./resolve')

let lib = findout.bind(this)

Object.assign(lib, {
  resolve
})

module.exports = lib
