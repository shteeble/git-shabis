/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const create = require('./create')
const Objnest = require('./objnest')

let lib = create()

Object.assign(lib, create, {
  create,
  Objnest
})

module.exports = lib
