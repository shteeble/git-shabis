'use strict'

const iftype = require('iftype')

function CustomType () {
}

{{#each is}}
// ----------------------------
// Check if type is {{{type}}}
// ----------------------------
{{#each tests}}
iftype({{{arg}}}).is({{{../type}}}) // => {{result}}
{{/each}}


{{/each}}