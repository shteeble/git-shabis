/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const IfType = require('./iftype')
const is = require('./is')
const create = require('./create')

let lib = create.bind()
lib.create = create
lib.IfType = IfType

lib.is = is
{{#each types}}
lib.is{{pascalcase this}} = is.bind(lib, '{{this}}')
{{/each}}

module.exports = lib
